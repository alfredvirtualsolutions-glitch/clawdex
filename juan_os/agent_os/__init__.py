"""Juan OS / VBS Local Agent OS — multi-agent workforce architecture.

A local-first, signal-first retirement prospecting operating system. This package
contains the agent catalog (the command hierarchy), the entity/status model, a
local-first SQLite store, an orchestration state machine, and a FastAPI backend
(REST + WebSocket) that the React frontend consumes.
"""

__version__ = "0.1.0"
