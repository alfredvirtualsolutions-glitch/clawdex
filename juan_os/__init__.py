"""Juan Operating System — a self-contained Python build of the Juan OS dashboard.

Runs locally with FastAPI, reads live from Neon Postgres, renders the neon
command dashboard as self-contained HTML, and can export the analytics as a
static, shareable HTML report.
"""

__version__ = "1.0.0"
